autoLogin();

var parent = setLayout();

var deny = this.isEditDenied(user);
if (deny) {
   res.message = deny;
   res.redirect(user.cache.referer ? parent.members.href("login") : parent.href());
}

res.data.title = "Antville - " + parent.title;

res.data.body = this.renderSkinAsString("main");
