autoLogin();
checkIfLoggedIn(this.href(req.action));

var parent = setLayout();

var deny = this.isDenied(user);
if (deny) {
   res.message = deny;
   res.redirect(parent.href());
}

this.renderGoodielist(parseInt(req.data.start,10));

res.data.title = "Antville - " + parent.title;

res.data.body = this.renderSkinAsString("main");
