autoLogin();

var deny = this.isEditMembersDenied(user);
if (deny) {
   res.message = deny;
   res.redirect(user.cache.referer ? this.members.href("login") : this.href());
}

res.skin = "weblog.page";

res.data.title = "Antville - " + this.title + " - list of members";

res.data.body = this.renderSkinAsString("memberlist");

