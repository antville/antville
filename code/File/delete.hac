checkIfLoggedIn(this.href(req.action));

var deny = this.isEditDenied(session.user);
if (deny) {
   res.message = deny;
   res.redirect(this.weblog.goodies.href());
}

if (req.data.submit == "cancel" || req.data.cancel)
   res.redirect(this.weblog.goodies.href());
else if (req.data.submit == "delete" || req.data.remove) {
   res.message = this.weblog.goodies.deleteGoodie(this);
   res.redirect(path.goodiemgr.href());
}

res.data.action = this.href(req.action);

res.data.title = this.weblog.title;

var skinParam = new Object();
skinParam.what = "the goodie <b>" + this.alias + "</b>";

res.data.body = this.renderSkinAsString("delete",skinParam);

path.weblog.renderSkin("page");