if (session.user) {
  res.message = "Good bye, " + session.user.name + "! Lookin' forward to seeing you again!";
  session.logout();
  session.data.referrer = null;
  res.setCookie ("avUsr", "");
  res.setCookie ("avPw", "");
}
res.redirect(this._parent.href());
