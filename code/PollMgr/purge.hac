for (var i=this.size(); i>0; i--) {
	var p = this.get(i-1);
	res.writeln(p);
	for (var n=p.size(); n>0; n--) {
		var c = p.get(n-1);
		res.writeln(c);
/*
		for (var x=c.size(); x>0; x--) {
			var v = c.get(x-1);
			res.writeln(v);
			v.setParent(c);
			c.remove(v);
		}
		c.setParent(this);
		this.remove(c);
*/
	}
//	p.setParent(weblog);
//	this._parent.remove(p);
}
